let sqr = (n) =>{
    return n*n;
}
//when a single argument is passed, parenthesis are optional


//Implicit return

let cube = (n) => (n*n*n);

//when a single value return by a arrow function, no need to write return statement

